version: '2'

services:
  app:
    build:
      context: .
    ports:
        - "80:80"
  mysql:
    image: mysql
    environment:
        - MYSQL_ROOT_PASSWORD=1234
    volumes:
        - ./data/mysql:/var/lib/mysql
    ports:
        - "3306:3306"
